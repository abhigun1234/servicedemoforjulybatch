import { MydireDirective } from './mydire.directive';

describe('MydireDirective', () => {
  it('should create an instance', () => {
    const directive = new MydireDirective();
    expect(directive).toBeTruthy();
  });
});
