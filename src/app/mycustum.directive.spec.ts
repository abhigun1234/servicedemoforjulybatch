import { MycustumDirective } from './mycustum.directive';

describe('MycustumDirective', () => {
  it('should create an instance', () => {
    const directive = new MycustumDirective();
    expect(directive).toBeTruthy();
  });
});
